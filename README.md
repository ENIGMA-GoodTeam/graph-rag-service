## Основные риски и способы их минимизации

Проект довольно амбициозный для хакатона: email-парсинг + LLM-структурированный вывод + граф знаний + UI с аппрувом + фоновая обработка.  
Ниже — самые критичные риски и как мы пытаемся их обойти.

| # | Риск | Вероятность | Последствия | Как минимизируем |
|---|------|-------------|-------------|------------------|
| 1 | LLM нестабильно извлекает структурированные поля (ФИО, серийные номера, телефон и т.д.) | Высокая | Пустые/мусорные значения в таблице → демо выглядит сломанным | Используем **строгий PydanticOutputParser** + **примеры few-shot** в промпте + fallback на "не удалось извлечь" с флагом ручной обработки |
| 2 | IMAP-парсинг ломается на реальных письмах (MIME, вложения, кодировки, разные клиенты) | Высокая | Письма вообще не попадают в систему | Ограничиваемся **только text/plain и text/html** частями, игнорируем вложения. Тестируем на 7–10 типовых письмах заранее. |
| 3 | GraphRAG / LLM отвечает слишком долго (> 15–20 сек) | Средняя | Оператор засыпает, демо тормозит | Выбираем **быструю модель** (Groq Llama-3.1-70B или Ollama 8–14B), кэшируем ответы на одинаковые письма, делаем optimistic UI (показываем "Обработка..." сразу) |
| 4 | Оператор не успевает аппрувить → SMTP не отправляется на демо | Средняя | Жюри видит "всё в статусе pending" | Готовим **2–3 уже обработанных и отправленных** письма в базе на момент демо + показываем историю отправленных |
| 5 | Проблемы с аутентификацией IMAP/SMTP (2FA, app password, OAuth) | Высокая | Нет ни одного письма в системе | Используем тестовый почтовый ящик **без 2FA** (создаём новый за 5 минут), храним credentials только в `.env`, проверяем подключение на CI/CD или локально заранее |
| 6 | Frontend отваливается при большом количестве записей / плохом UX | Средняя | Жюри не может пользоваться таблицей | Делаем **минимальную таблицу** (TanStack Table + виртуализация), без сложных фильтров на первом этапе, добавляем пагинацию |
| 7 | Разные LLM-провайдеры падают / кончаются лимиты | Средняя | Всё работает, кроме AI-части | Готовим **fallback-режим**: если LLM не отвечает → ставим статус "AI failed" + показываем сырой текст письма |
| 8 | Не успеваем сделать Telegram/Slack-уведомления | Низкая | Не критично, но красиво | Делаем опциональным — если не успеем, просто выключаем. Основной фокус — таблица + аппрув + отправка |

### Принципы выживания на хакатоне

- Делаем **MVP за первые 12–16 часов**: IMAP → парсинг → таблица → ручная отправка письма
- Всё остальное (GraphRAG, авто-генерация ответа, sentiment, аппрув в один клик) — улучшаем по мере возможности
- Подготавливаем **демо-скрипт** с 4–5 заранее созданными письмами и записанными в БД тикетами
- Держим **локальный режим без интернета** (Ollama) как запасной вариант на случай проблем с Groq/Yandex/Grok

Если что-то из списка выше сломается критически — будем выключать эту часть и показывать, что "остальное работает стабильно".

Удачи нам! 🚀
